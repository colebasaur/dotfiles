return {
  "mason-org/mason-lspconfig.nvim",
  version = "v1.32.0", -- Pin to the last version before the breaking change
  dependencies = {
    "mason-org/mason.nvim",
    "neovim/nvim-lspconfig",
  },
}
