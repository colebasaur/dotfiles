# Copy this file to .global.env and fill in values for your environment.
# .global.env is gitignored — never commit it.

# GitLab
export GITLAB_TOKEN=""
export GITLAB_GROUP_ID=""
export GITLAB_URL="https://gitlab.com"
export GITLAB_DEPLOY_TOKEN=""

# YouVersion internal tooling
export YV_DOCKER_CACHE=""
export YV_REGISTRY=""
export YVP_API_KEY=""

# Flutter (optional — only needed for Flutter projects)
export FLUTTER_REPOSITORY_TOKEN=""
